*.egg-info
data
hub
.idea
build
*.sw*
blunt.egg-info
dist
*.pyc
__pycache__/
docs/*
venv
.venv

# workspace scratch directory — contains build artifacts and full repo copies
_/

# Google OAuth secrets — never commit (contain client secret / refresh token)
credentials.json
token.json
